# 2026-09-09 · CSS 归属整理接续

## 范围与结果

在 `eaad5bc` 状态与持久化整理提交上进行第二阶段重构，工作分支为 `codex/css-ownership`。当前仍是本地前端 Demo，不接入后端、不部署。

用户要求现有 Demo 作为正式产品视觉基准，组件化与模块化不能造成两套界面的风格差异。已将“复用同一套前端源代码、按主题与组件顺序改版”的约定写入根 README、HANDOFF 与 [前端样式约定](frontend-styles.md)。该文档包含完整组件/样式映射和后续修改顺序。

## 已完成

- `globals.css` 收敛为框架、元素默认值、输入方式与减少动态效果规则，约 150 行。
- 新增 `theme.css`，集中纸张、文字、强调色、共用间距、浮层层级及主要动画参数，保留已有框架 token 映射。
- 新增 `styles.css`，固定加载共用控件、外壳、首页和各章节的样式。移除 TSX 中分散的三个 CSS import，避免挂载顺序影响层叠。
- 原 `journal.css` 的规则与 `globals.css` 中的产品样式迁入组件旁的 CSS 文件，并删除旧大覆盖文件。
- 盘点并移除 606 条被后续相同选择器、相同条件或无条件声明覆盖的属性；移除无使用方的旧侧栏、旧首页介绍等选择器。短写/长写、交互状态和不同断点下仍有效的规则保留。
- 保持类名、DOM、状态命令和交互行为。颜色变量只替换完全相同的值，未合并近似色；时间轴刻度、封面比例和 Note 控件尺寸等仍属于相应组件。

## 验证

- 37 项 Node 测试通过。
- TypeScript、应用代码范围 lint、格式检查及 `git diff --check` 通过。
- Vinext 生产构建通过。
- 现有 `layout-motion.mjs`、`responsive-interactions.mjs`、`workspace-controls.mjs`、`inbox-note-controls.mjs`、`memory-composer.mjs` 通过；覆盖封面与章节进入、桌面时间轴、键盘与减少动态效果、手机滚动边界、Note 控件、小猫拖动和位置保存、记忆包编辑与鼠标/触屏排序。
- 新增 `style-parity.mjs`。从原始 `eaad5bc` 单独启动基线，用同一脚本、固定日期和独立浏览器上下文采集；与本次生产构建比较，320 / 390 / 768 / 1280px 共 56 个状态全部通过。
- 视觉检查包括元素矩形、常用计算样式、伪元素和固定视口 PNG。处理了无背景图时生产压缩器的等价零值写法，以及截图时浏览器重排/合成的不稳定因素。弹窗原始模糊参数严格比较；仅 PNG 采集时临时关闭背景模糊并等待重绘，像素通道容差为 4/255。具体方法与复现命令见样式约定。

## 已知限制与下一步

- 第一阶段已确认的 `ui-interactions.mjs` 触屏惯性断言失败仍是既有问题；本轮未修改时间轴手势逻辑，也未放宽该断言。这里的通过结果不包含该脚本。
- 实体 iPhone/Safari 仍待验收；本机 Chrome 与字体环境的截图不代表所有设备。
- 后续进入第三阶段，先拆原文时间轴/轮次详情、摘要工作台、记忆包排序/编辑窗口等复杂组件。沿用这里的样式责任范围和第一阶段的命令接口，保持单一前端来源，每次迁移后对照同一视觉基准。
- 不要重新建立 `journal.css` 式的大覆盖文件；风格更新先改主题，再共用控件，再外壳与各章节。
