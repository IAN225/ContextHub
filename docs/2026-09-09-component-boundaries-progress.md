# 2026-09-09 第三阶段接续：复杂组件拆分

从第二阶段 `a83e48d` 开始，在 `codex/component-boundaries` 分支按交互职责拆分原文、摘要、记忆包与导入确认。全程未使用子智能体。当前仍是本地前端 Demo，未连接真实后端、模型或部署服务。

## 已完成

- 原文拆为章节协调、导航 hook、时间轴展示和轮次详情；视图偏好和导航的挂载周期保持在章节层。
- 摘要设置、工作台和回退确认独立，摘要页继续持有检查点与任务入口；回退窗口供候选确认直接复用。
- 记忆包排序提取为只接收块数组的 hook，卡片和编辑窗口各自独立；保留实时补位、取消恢复和源数据隔离。
- 导入窗口、收件审阅、完整轮次预览和归档选择分别归属；页面直接导入使用的入口。
- 保持现有 DOM、类名、CSS、存储键、领域规则与命令接口。没有新增依赖，也没有引入第二套前端。
- 新增 [组件职责与状态边界](frontend-components.md)，同步 [样式归属](frontend-styles.md) 与接续文档。

## 验证

- 37 项 Node 测试、TypeScript、应用范围 lint、格式检查和生产构建通过。
- 既有六组浏览器回归通过：`layout-motion.mjs`、`responsive-interactions.mjs`、`workspace-controls.mjs`、`inbox-note-controls.mjs`、`memory-composer.mjs`、`state-persistence.mjs`。
- 新增 `component-lifecycle.mjs`，对本地生产构建检查原文视图保留、草稿恢复、摘要候选 keep 模式、完整轮次归档、取消排序和 Note 查询重置。测试使用独立浏览器数据，保留已有示例收件，等待异步草稿读取完成再断言。
- 拆分前在 `a83e48d` 采集基线，拆分后使用生产预览比较：320 / 390 / 768 / 1280px 的 56 个状态全部通过。比较元素矩形、计算样式、伪元素与视口 PNG；沿用第二阶段已记录的截图稳定化与像素容差，没有重置基线或放宽条件。

## 下一阶段

第四阶段先测量长对话节点数、搜索、持久化频率和隐藏章节任务，再决定虚拟化或缓存方案。维持同一前端来源及本地范围。

此前 `ui-interactions.mjs` 的触屏惯性断言问题仍单独保留，本阶段没有修改手势算法或放宽该断言；本次通过结果不包括这个脚本。实体 iPhone/Safari 的惯性、吸附与截停仍需设备验收。
